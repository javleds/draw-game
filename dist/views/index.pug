doctype html
head
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0')
    meta(http-equiv='X-UA-Compatible' content='ie=edge')
    title Diviertete
h1 Bienvenido!

if error && error === 'emptyNik'
    p El apodo no puede estar vacío.

if error && error === 'emptyCode'
    p El código no puede estar vacío.

if error && error === 'invalidCode'
    p El código #{code} no existe.

p Si tienes el código de una partida ingresalo aqui:
form(action="/game")
    .form-group
        label(for="nikJoin") Apodo:
        input(required type=text id="nikJoin" name="nik")

    .form-group
        label(for="code") Código:
        input(required type=text id="code" name="code")

    .form-group
        button(type="submit") Entrar

p De lo contrario, crea una partida

form(action="/create")
    .form-group
        label(for="nikCreate") Apodo:
        input(required type=text id="nikCreate" name="nik")

    .form-group
        button(type="submit") Crear

script(src="/socket.io/socket.io.js")
